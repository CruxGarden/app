# Notices

Glyphr Studio 2 — Copyright © 2010–2026 Matthew LaGrandeur — GPL-3.0-or-later (`LICENSE-gpl-3.0.txt`).

Runtime dependencies bundled into the app (each declares its license in its package.json; the packages ship no separate license file):

- `font-flux-js` — GPL-3.0-only — Matthew LaGrandeur
- `bezier-boolean` — GPL-3.0-only — Matthew LaGrandeur
- `svg-to-bezier` — GPL-3.0 — Matthew LaGrandeur
- `@mattlag/xmltojson` — GPL-3.0 — Matthew LaGrandeur

The Crux Garden adaptation (`src/garden/`, `garden/`) is distributed under the same GPL-3.0-or-later terms as part of this fork; Crux Garden itself is a separate program that embeds the built app in a frame and exchanges messages with it.
