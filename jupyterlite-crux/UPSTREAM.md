# JupyterLite in Crux Garden

Actual JupyterLite 0.8.3 and its native JupyterLab interface, with jupyterlite-pyodide-kernel 0.8.3 and Pyodide 314.0.4. The frontend is built using the upstream Python distribution; Garden customizes its configuration and supplies a persistence adapter, keeping the native notebook, cell, file-browser and export workflows.

Upstream sources: https://github.com/jupyterlite/jupyterlite/tree/v0.8.3 , https://github.com/jupyterlite/pyodide-kernel/tree/v0.8.3 , https://github.com/pyodide/pyodide/tree/314.0.4 . Preserve LICENSE, vendor/PYODIDE-LICENSE, vendor/CPYTHON-LICENSE, runtime/build/third-party-licenses.json and the kernel extension's notices. Python wheels include their own licenses; the runtime bundler also checks the recorded hashes.

Native memory storage is hydrated from the Crux. Each notebook or dataset has a separate fingerprinted JSON Artifact; data/project.json preserves the file hierarchy and open documents. Save flushes native document models, then confirms the Garden write. Kernel memory, native temporary checkpoints and UI preferences are separate from saved notebooks; code, Markdown, outputs, imported files and folders travel with the Crux and Growth. Kernels restart on reopen. Native .ipynb and file downloads remain available; whole-editor website publication is unavailable.

Python, NumPy, Matplotlib and their core dependencies are bundled. A baseline calculation and PNG plot execute with external HTTP requests blocked. Additional Python packages and user-authored network requests may still need connectivity. This is browser Python, not a full native Python environment.

Rebuild with Python 3.10+: `python3 scripts/build.py` (or `npm run build`). The build creates a local .build-venv and cache excluded by .cruxignore, installs the pinned build dependencies, builds upstream JupyterLite, checks the bundled runtime hashes and installs the Garden bridge. Source files in garden/ and scripts/ are editable. Build tools download dependencies; installed runtime operation does not invoke Python on the host.

The Garden service tests cover complete Crux export/import, notebook outputs, folder metadata and Growth restore. Desktop acceptance covers native notebook creation/execution, a NumPy calculation and Matplotlib plot, CSV upload and Python file reads/writes with external HTTP blocked, scoped agent Markdown insertion, native .ipynb download, external conflict/reload and restart with the saved plot and cells followed by fresh Python execution reading the persisted result file. An app-created Crux also rebuilds from its packaged source; the clean Docker build is verified. See app/electron/e2e/jupyterlite-app.spec.ts in Garden source.

## Notebook analysis commands (2026-09-14, workflow verified)

All new adaptations are in `garden/`; upstream notebook, cell and kernel source is unchanged. These commands ship in newly created notebook Cruxes; existing Project Folders keep their own runtime/source and are not silently upgraded. `garden/notebook.js` prepares operations against the open native notebook. `garden/commands.js` shares bounded validation with Garden's host tools. `garden/shared/command-session.js` is the portable copy of Garden's common serialized, confirmed command lifecycle. Rebuilds package these files through the existing configure script; no hosted helper is needed.

Native seams and manual equivalents:

- `ContentsManager.get/save` and `docmanager:open`: create a named notebook without overwriting a file, equivalent to New Notebook and Rename in the file browser. File creation is not a cell Undo operation.
- `sharedModel.cells`, cell `getId/getSource/getOutputs` and `setSource`: inspect and replace one unique passage, preserving metadata, attachments and other source. Source editing uses the native cell history; existing outputs are explicitly reported as potentially stale. Pagination bounds text and omits binary/HTML payloads.
- `sharedModel.insertCell` and its Undo manager: insert code/Markdown cells, equivalent to native cell insertion. History capture boundaries separate an agent operation from nearby manual typing.
- Native `notebook:move-cell-up/down` and `notebook:delete-cell`: move/delete only the selected target. Native cell Undo/Redo remains available. IDs are re-resolved after the confirmed pre-save, refusing missing cells or changed exact text.
- Native `notebook:run-cell`: execute only the selected cell, preserving JupyterLab's output/error rendering. Returns the success boolean and bounded output evidence; Python errors remain saved notebook results. A 60-second timeout attempts kernel interruption and tells the caller to inspect before retrying. Python execution/file writes and kernel memory are not undone by cell Undo, and kernel memory is not persisted. Dependencies must be rerun after restart.
- Native notebook `model.toJSON` (File > Download equivalent) saves `.ipynb` outputs. Existing code-cell `image/png` results can be saved as PNG outputs, equivalent to saving the native rendered image. Neither output command reruns code. Both use Garden's existing confirmed output service.

The ordinary workflow is a small CSV analysis: create, inspect, run, correct a column error, plot, preserve a person's Markdown note, revise/reorder cells, and export a notebook plus PNG. Uploaded files use the native file browser; general host Artifacts are not automatically mounted into the kernel filesystem. Arbitrary kernel/package management, interactive stdin, batch run-all and every notebook command are outside this slice.

Upgrade checks: verify native IDs and shared-model methods, the document/cell Undo configuration, selected-cell command targeting and boolean execution results, timeout interruption, native buffers captured on immediate save, saved outputs and notebook contents after restart, and an isolated complete Crux import that re-executes with external HTTP blocked. Model/command unit tests and packaging checks are additional evidence, not substitutes for the native journey. The reference journey lives at `electron/e2e/jupyterlite-depth.spec.ts` in Garden source; the existing conflict/reload journey must continue to pass.

Verification: the complete desktop analysis journey passes, including native text and cell Undo/Redo, a real Python error/correction and fresh offline execution after complete import. The existing native conflict/reload/download/restart journey passes. Full app and Electron gates pass; the host gate reports 1,086 tests. Evidence and limits are in `app/docs/notebook-depth/README.md` in Garden source.
