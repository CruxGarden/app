# JSCAD (Model Crux)

JSCAD's web application as a Crux Garden creation tool. See `UPSTREAM.md` for the vendored release and the Garden bridge.

```sh
npm run check         # syntax of the bridge, the validator and the starter
npm run test:garden   # the document validator
```
