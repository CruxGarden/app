# Blender Garden-agent trial

2026-09-15: real Claude Code provider launched from Garden Collaboration, reviewed community Blender MCP connection, native Blender 5.2 disposable GUI scene. `garden-agent-turn.json` contains the completed save/export turn; `scene.blend`, `sprout.png` and `sprout.glb` are the actual returned files. The first modeling turn timed out during external approval review; resuming the same Crux completed in 1.8 minutes. The render still has a visible studio corner and simple leaves; this is integration evidence, not finished demo art.

GLB JSON independently contains eight meshes and only the pot/soil/stem/leaves, excluding camera/lights/backdrop. The live agent exported using the native export_scene tool. Project-scoped MCP configuration was required; a CLI-local registration was invisible to the SDK provider. The server is enabled by name only in the disposable project, with telemetry disabled and safe mode enabled. Native add-on status inspection failed because the bootstrap module name differs from installed add-on packaging; bridge operations succeeded.

Restart/Growth preservation and the full app/Electron verification gates pass. Exact GLB transfer with saved-scene provenance and all eight meshes rendering in native GDevelop are verified. The game play test currently fails at keyboard movement; gameplay, Astro export and complete fresh-Cruxspace replay remain pending. Multiple Blender processes correctly refuse window arrangement; positive single-session placement, manual contribution preservation and native Blender continuation after portable import remain unverified. This does not certify general agent quality or every Blender operation.
