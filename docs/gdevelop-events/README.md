# GDevelop native event collaboration

2026-09-15. Verified native engine, host and isolated desktop integration checkpoint; 23 GDevelop tools total.

Four tools add paginated event/instruction inspection, nested event organization, targeted condition/action editing and native event Undo/Redo. Public tools use `eventPath` for native indexes because the host reserves `path` for Artifact paths. Three narrow EventsSheet registration hooks connect an isolated Garden adapter to native state/history/tree rendering. The portable source includes the adapter and native command module.

Native engine tests cover nested organization, movement within/across lists, preserved manual comments/variables, instruction metadata/arity, child conditions and invalid operations leaving the real project unchanged. Commands are validated on an owned native EventsList copy. Native expressions and object references still need runtime verification.

The desktop journey creates a comment through Collaboration, edits it manually in the native sheet, refuses a stale change, creates and revises nested rules, duplicates/moves/removes events and combines native toolbar Undo with tool Undo/Redo. The resulting start-of-scene condition and position action must set the sprite's runtime X to 123, with native keyboard movement still working. It checks imported rules before repeating manual/tool revisions in a fresh Garden, while retaining the existing object/instance, native exports, independently booted game, conflict/reload, restart and 3D checks. Evidence: [native sheet](native.png), [imported sheet](portable.png), [native calls](native-calls.json), [imported calls](portable-calls.json).

The first desktop run stopped on the stale-edit negative check because an array argument named `path` reached the host's file-path normalization. The public schema/mapping was changed to `eventPath` and has a regression check; native commands and file-path safety are unchanged. A separate mock scenario correction has a red/green regression. The second desktop run passed all 32 native and 22 imported event calls (one deliberate stale refusal in each), preserved the manual comments, exercised native/tool history and verified the rule during play. It then stopped on an older unscoped object-name selector because the hidden event sheet also contains NewSprite; object editing/placement selectors now target #objects-list. The final complete journey passed in 7.1 minutes (7.2 with runner setup). Both final screenshots and all 54 returned event calls were inspected; each sequence has one deliberate stale refusal and otherwise succeeds. Existing instance/object editing, native instance/event history, manual editing, original media, both native ZIPs, independently booted export, restart, clean complete-Crux import and native 3D toolbar/Mesh checks pass. No captured page/debugger crashes; three unlocated resource 404 console messages remain. The fixture cleanup removed two older temporary Gardens under its existing retention policy.

Full app verification passed all bundled gates/builds, seventeen native GDevelop tests and 1,109 host tests. After the public argument/mock corrections, final typecheck/lint, all 1,111 host tests across 191 files and production build (1m 32s) passed; Electron verification passed. The final desktop-only selector correction needed no app rebuild. This is deterministic integration testing, not a real-model collaboration evaluation.

Limits: creation supports standard/comment/group events. Existing nested containers and conditions/actions are editable; loop/link/JavaScript configuration, while-guard lists and event variables remain separate. The older add-event operation still has its prior path pending a deliberate compatibility-tested migration. Event history is separate from instance history. Each mutation needs fresh inspection; real-model evaluation must assess usefulness within the turn's tool-loop budget.
