/** Background animation type */
export enum BgType {
  Bloom = 'bloom',
  Drift = 'drift',
  Flow = 'flow',
  Blank = 'blank',
  Image = 'image',
}

/** Theme mode — what the user selects */
export enum ThemeMode {
  Dark = 'dark',
  Light = 'light',
  Auto = 'auto',
}

