// Upstream keeps this module out of the repository (its analytics endpoint is private).
// Crux Garden records nothing: the FM synth demo's events are dropped.
export const logEvent = (_name: string, _data?: unknown): void => {};
