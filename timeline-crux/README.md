# TimelineJS (Timeline Crux)

Knight Lab's TimelineJS as a Crux Garden creation tool, with the smallest editor around it. See `UPSTREAM.md`.

```sh
npm run check         # syntax of the bridge, the editor, the validator and the starter
npm run test:garden   # the document validator
```
