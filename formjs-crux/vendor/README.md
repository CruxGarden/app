# form-js (bpmn.io) — vendored build

`@bpmn-io/form-js` 1.26.0 as published on npm: `form-editor.umd.js` (the form builder), `form-viewer.umd.js` (the viewer the public edition uses), `form-js-editor.css`, `form-js.css`, `flatpickr/light.css`. Copyright Camunda Services GmbH, MIT with the bpmn.io watermark clause (LICENSE): the small "powered by" mark the editor and viewer render must stay visible. Upstream: https://github.com/bpmn-io/form-js. Nothing here is modified.
